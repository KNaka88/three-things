import { ConfirmWaitingPipe } from './confirm-waiting.pipe';

describe('ConfirmWaitingPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmWaitingPipe();
    expect(pipe).toBeTruthy();
  });
});
